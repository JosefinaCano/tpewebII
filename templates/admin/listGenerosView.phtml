<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Administrar catálogo</title>
    <link rel="stylesheet" href="<?= BASE_URL ?>css/list.css">
</head>
<body>
    <h1>Discdogs</h1>

    <p>
        <a href="<?= BASE_URL ?>admin/addGenero" class="btn-add">Agregar Género</a>
    </p>

    <?php if (!empty($generos)): ?>
        <table border="1" cellpadding="10" cellspacing="0">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($generos as $genero): ?>
                    <tr>
                        <td><?= $genero->id ?></td>
                        <td><?= htmlspecialchars($genero->nombre) ?></td>
                        <td>
                            <a href="<?= BASE_URL ?>admin/deleteGenero/<?= $genero->id ?>"
                               onclick="return confirm('¿Seguro que querés eliminar este género?')"
                               class="btn-delete">
                                Eliminar
                            </a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    <?php else: ?>
        <p>No hay géneros para mostrar.</p>
    <?php endif; ?>

    <p><a href="<?= BASE_URL ?>home" class="back-button">Volver al Home</a></p>
    <p><a href="<?= BASE_URL ?>admin" class="back-button">Volver al administrador</a></p>


</body>
</html>
